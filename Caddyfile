{
    frankenphp {
    }
}

:{$PORT:8080} {
    root * public/

    php_server {
        env DATABASE_URL {env.DATABASE_URL}
    }

    @not_file {
        not file {path}
        not path *.php
    }
    rewrite @not_file /index.php?{query}

    file_server
}