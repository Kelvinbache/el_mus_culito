<IfModule mod_rewrite.c>
    RewriteEngine On

    RewriteRule ^$ public/ [L]

    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]

    RewriteRule ^(config|src|views|core|.env) - [F,L]
</IfModule>